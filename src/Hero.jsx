import React from "react";


const heroStyles = {
  display: "flex",
  justifyContent: "space-between",
  padding: "20px",
};

const heroLeftStyles = {
  flex: 1,
};

const heroRightStyles = {
  flex: 1,
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};

const handIconStyles = {
  display: "flex",
  alignItems: "center",
};

const heroLatestBtnStyles = {
  display: "flex",
  alignItems: "center",
  cursor: "pointer",
};

const Hero = () => {
  return (
   
  );
};

export default Hero;
